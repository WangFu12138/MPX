# Data 目录说明

`data` 目录用于管理和加载各类机器人的**物理形态学资源文件 (URDF/MJCF 等)**。这些模型是基于仿真器（如 MuJoCo）进行动力学结算推导的物理承载实体。

## 关键子目录流：

- **`r2-1024/` 等子目录**:
  包含了类似于 Unitree R2 或其他相关机械骨架的完整 XML 构建配置（包括传感器、自由度限制、碰撞网格与视觉渲染网格等）与资源包裹信息（如 `CMakeLists.txt`, `package.xml` 等外围框架导出设置）。
